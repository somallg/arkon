---
# Backend image built once, reused by api + workers
x-backend: &backend
  build:
    context: .
    dockerfile: Dockerfile
  image: arkon-backend:latest
  restart: always
  depends_on:
    arkon-postgres:
      condition: service_healthy
    arkon-redis:
      condition: service_healthy
    arkon-minio:
      condition: service_healthy
  volumes:
    - ./temp_uploads:/app/temp_uploads

services:
  # ── Infrastructure ────────────────────────────────────────────
  arkon-postgres:
    image: pgvector/pgvector:pg16
    container_name: arkon-postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-arkon}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-arkon_secret}
      POSTGRES_DB: ${POSTGRES_DB:-arkon}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # ports:
    #   - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-arkon} -d ${POSTGRES_DB:-arkon}"]
      interval: 5s
      timeout: 5s
      retries: 10

  arkon-redis:
    image: redis:7-alpine
    container_name: arkon-redis
    restart: always
    command: redis-server --save 60 1 --loglevel warning --requirepass ${REDIS_PASSWORD:-arkon_secret}
    volumes:
      - redis_data:/data
    # ports:
    #   - "6379:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a ${REDIS_PASSWORD:-arkon_secret} ping | grep PONG || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10

  arkon-minio:
    image: minio/minio:latest
    container_name: arkon-minio
    restart: always
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin123}
    volumes:
      - minio_data:/data
    ports:
      - "9002:9000"
      - "9003:9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 10

  # ── Application ───────────────────────────────────────────────
  arkon-migrator:
    <<: *backend
    container_name: arkon-migrator
    command: alembic upgrade head
    restart: "no"

  arkon-api:
    <<: *backend
    container_name: arkon-api
    depends_on:
      arkon-migrator:
        condition: service_completed_successfully
      arkon-postgres:
        condition: service_healthy
      arkon-redis:
        condition: service_healthy
      arkon-minio:
        condition: service_healthy
    ports:
      - "5055:5055"
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 5055 --proxy-headers --forwarded-allow-ips='*'
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:5055/health')"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  arkon-worker:
    <<: *backend
    container_name: arkon-worker
    command: python -m arq app.worker.WorkerSettings
    depends_on:
      arkon-api:
        condition: service_healthy

  arkon-worker-skills:
    <<: *backend
    container_name: arkon-worker-skills
    command: python -m arq app.worker.SkillWorkerSettings
    depends_on:
      arkon-api:
        condition: service_healthy

  arkon-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:5055}
    image: arkon-frontend:latest
    container_name: arkon-frontend
    restart: always
    ports:
      - "3119:3000"
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:5055}
      - INTERNAL_API_URL=http://arkon-api:5055
    depends_on:
      arkon-api:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  minio_data:
